#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char nic;

	for (nic = 'a'; nic <= 'z'; nic++)
		putchar(nic);
		printf("\n");

	return (0);
}
