#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char y;

	for (y = 'a'; y <= 'z'; y++)
		putchar(y);
		putchsr('\n');

	return (0);
}
